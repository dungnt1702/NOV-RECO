# Production Environment Configuration
# Set these environment variables on your production server

# Django Settings (REQUIRED - Change these!)
DJANGO_SECRET_KEY=nov-reco-production-secret-key-change-this-in-real-production
DJANGO_DEBUG=0
DJANGO_ENVIRONMENT=production

# Database Configuration (SQLite for simple production)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=/var/www/checkin.taylaibui.vn/data/db.sqlite3

# PostgreSQL alternative (uncomment if using PostgreSQL)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=nov_reco_checkin_prod
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=nov_reco_user
# DATABASE_PASSWORD=your-secure-database-password

# Server Configuration
ALLOWED_HOSTS=checkin.taylaibui.vn,www.checkin.taylaibui.vn,103.15.51.66,localhost,127.0.0.1
SERVER_PORT=8000

# Site Configuration
SITE_ID=1
LANGUAGE_CODE=vi
TIME_ZONE=Asia/Ho_Chi_Minh

# Static Files (for production with web server)
STATIC_URL=/static/
STATIC_ROOT=/var/www/nov-reco/staticfiles/
MEDIA_URL=/media/
MEDIA_ROOT=/var/www/nov-reco/media/

# Email Configuration (SMTP for production)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=1

# Google OAuth (Production credentials)
GOOGLE_OAUTH_CLIENT_ID=your-production-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-production-google-client-secret

# Security Settings (strict for production)
SECURE_SSL_REDIRECT=1
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=1
SECURE_HSTS_PRELOAD=1
SECURE_CONTENT_TYPE_NOSNIFF=1
SECURE_BROWSER_XSS_FILTER=1
SESSION_COOKIE_SECURE=1
CSRF_COOKIE_SECURE=1

# Logging Level
LOG_LEVEL=INFO

# Additional Production Settings
USE_HTTPS=1
FORCE_SCRIPT_NAME=