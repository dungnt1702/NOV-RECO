# Test/Staging Environment Configuration
# For testing on checkin.taylaibui.vn server (103.15.51.66)

# Django Settings
DJANGO_SECRET_KEY=nov-reco-test-secret-key-for-checkin-taylaibui-vn-server
DJANGO_DEBUG=1
DJANGO_ENVIRONMENT=test

# Database Configuration (SQLite for test)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=/var/www/checkin.taylaibui.vn/data/db_test.sqlite3

# Server Configuration  
ALLOWED_HOSTS=checkin.taylaibui.vn,www.checkin.taylaibui.vn,103.15.51.66,localhost,127.0.0.1
SERVER_PORT=8000

# Site Configuration
SITE_ID=1
LANGUAGE_CODE=vi
TIME_ZONE=Asia/Ho_Chi_Minh

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/var/www/checkin.taylaibui.vn/staticfiles/
MEDIA_URL=/media/
MEDIA_ROOT=/var/www/checkin.taylaibui.vn/media/

# Email Configuration (Console for test)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Security Settings (disabled for test environment to avoid 301 redirects)
SECURE_SSL_REDIRECT=0
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=0
SECURE_HSTS_PRELOAD=0
SECURE_CONTENT_TYPE_NOSNIFF=0
SECURE_BROWSER_XSS_FILTER=0
SESSION_COOKIE_SECURE=0
CSRF_COOKIE_SECURE=0
USE_HTTPS=0

# Logging Level
LOG_LEVEL=DEBUG

# Test specific settings
USE_HTTPS=0
FORCE_SCRIPT_NAME=